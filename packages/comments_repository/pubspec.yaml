name: comments_repository
description: A Very Good Project created by Very Good CLI.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.verygoodcore
        pluginClass: CommentsRepositoryPlugin
      ios:
        pluginClass: CommentsRepositoryPlugin

dependencies:
  flutter:
    sdk: flutter
  pigeon: ^22.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  plugin_platform_interface: ^2.1.8
  very_good_analysis: ^7.0.0
